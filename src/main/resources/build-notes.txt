
// to temp set path for java 26
$env:Path = "C:\Users\sesa91827\.jdks\jdk-21.0.6-full\bin;" + $env:Path
$env:Path = "C:\Users\sesa91827\.jdks\jdk-21.0.10-full\bin;" + $env:Path

// to build
./gradlew clean build shadowJar generateRuntime packageApp

// to upgrade JDK
Add JDK
Set new JDK used in project structure
Change all entries in build.gradle.kts
In Windows, go to System Properties and change JAVA_HOME to new version

// how to show all tags that were made
git tag --sort=v:refname
v0.9.0
v0.9.8
v0.9.9
v1.0.0
v1.0.1
v1.0.3
v1.1.3
v1.2.0

// how to add a new tag
git tag -a v1.2.1 -m "Release v1.2.1"

git push origin v1.2.1

